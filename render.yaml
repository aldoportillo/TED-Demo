services:
  - type: web
    name: TED-Demo
    env: ruby
    plan: free
    buildCommand: "./bin/render-build.sh" 
    startCommand: "./bin/render-start.sh"
    envVars:
      - key: SECRET_KEY_BASE
        generateValue: true
